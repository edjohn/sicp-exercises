;Exercise 1.5
;With applicative-order evaluation, (p) would be evaluated and the program would loop indefinitely. 
;With normal-order evaluation, (p) would never be evaluated because the program ends after the first conditional check.
